/** Types for web/js/bbcode.js, which the tests exercise. */

/** A me! page (osu!'s BBCode) as HTML, or '' when there is nothing in it. */
export function bbcodeHtml(input: string | null | undefined): string;
